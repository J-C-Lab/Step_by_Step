# 工作日志

## 时间
2026-05-08

## 本次目标

根据 `edit5.md` 实现“代码可视化适配助手”功能：在左侧代码编辑区右下角新增按钮，帮助用户将普通算法片段整理为当前执行沙盒更容易运行、逐步调试和可视化的脚本。

---

## 实施内容

### 1. 新增需求文档

文件：`edit5.md`

- 定义“适配可视化”按钮的使用场景和功能边界。
- 明确 MVP 规则：
  - 顶层 `return` 转 `var result = ...`
  - `let/const` 转 `var`
  - 轻量格式化
  - 明显 `new Array(n)` + `result = arr[n]` 的越界访问修正为 `arr[n - 1]`
  - 保留关键中间变量，便于 Watch 和 ReactFlow 可视化。

### 2. 新增规则化适配工具

文件：`src/utils/codePrep.js`

新增 `prepareCodeForVisualization(code)`：

- 输入当前编辑器代码。
- 输出：
  - `code`：适配后的代码；
  - `messages`：本次修改说明；
  - `changed`：是否发生修改。
- 不依赖 React，不引入后端或外部 AI API。

当前规则：

- 将 `let` / `const` 替换为 `var`。
- 将顶层 `return expression;` 替换为 `var result = expression;`。
- 在明确模式下修复 `var result = dp[n];` 为 `var result = dp[n - 1];`。
- 做保守的轻量格式化，不改变算法逻辑。

### 3. 接入左侧代码编辑器 UI

文件：`src/components/CodeEditor.jsx`

- 在 Monaco 编辑器右下角新增 `适配可视化` 按钮。
- 点击按钮后：
  - 调用 `prepareCodeForVisualization(code)`；
  - 用 `onChange(result.code)` 写回编辑器；
  - 显示短提示说明本次做了哪些修改；
  - 自动把焦点还给 Monaco 编辑器。
- 保留之前的空格键输入兜底逻辑。

---

## 验证

### 示例验证

输入：

```javascript
var n = 5;
var dp = new Array(n).fill(0);
dp[0] = 1;
dp[1] = 1;
for(let i = 2; i < n; i++){
  dp[i] = dp[i - 1] + dp[i - 2];
}
return dp[n];
```

输出：

```javascript
var n = 5;
var dp = new Array(n).fill(0);
dp[0] = 1;
dp[1] = 1;
for (var i = 2; i < n; i++) {
  dp[i] = dp[i - 1] + dp[i - 2];
}
var result = dp[n - 1];
```

提示：

- 将 `let/const` 转为 `var`
- 将顶层 `return` 改为 `var result`
- 修正明显的 `dp[n]` 越界访问
- 做轻量格式化

### 工程验证

- 直接运行 `prepareCodeForVisualization()` 示例 smoke test：通过。
- `ReadLints` 检查：
  - `src/components/CodeEditor.jsx` 无新增诊断。
  - `src/utils/codePrep.js` 无新增诊断。
- `npm run build`：通过。

---

## 修改文件

- `edit5.md`
- `src/utils/codePrep.js`
- `src/components/CodeEditor.jsx`
- `logs/2026-05-08_TaskLog.md`

---

## 后续可扩展

- 引入更可靠的 AST 级分析，避免复杂代码下的正则误判。
- 增加“仅预览不写回”模式。
- 增加更多算法模板识别，例如递归、图遍历、链表构造。
- 给适配说明增加可关闭面板或历史记录。

---

## 追加记录（16:52 DP 示例只能执行两步）

### 用户反馈

适配后的 DP 示例虽然能进入沙盒，但只执行两步就结束，无法继续观察完整算法过程。

### 根因判断

问题出在：

```javascript
var dp = new Array(n).fill(0);
```

这类现代数组方法在当前 `js-interpreter` 沙盒环境里不够稳定，可能导致解释器快速结束或中断。语法适配成功并不等于运行时 API 一定可用。

### 修复内容

修改文件：`src/utils/codePrep.js`

新增规则：将 `new Array(n).fill(value)` 展开为 ES5 显式初始化循环。

例如：

```javascript
var dp = new Array(n).fill(0);
```

会被适配为：

```javascript
var dp = [];
for (var __init_dp = 0; __init_dp < n; __init_dp++) {
  dp[__init_dp] = 0;
}
```

这样沙盒会逐步执行初始化过程，`dp` 的变化也能被 Watch 和右侧可视化捕获。

### 修复后的示例输出

```javascript
var n = 5;
var dp = [];
for (var __init_dp = 0; __init_dp < n; __init_dp++) {
  dp[__init_dp] = 0;
}
dp[0] = 1;
dp[1] = 1;
for (var i = 2; i < n; i++) {
  dp[i] = dp[i - 1] + dp[i - 2];
}
var result = dp[n - 1];
```

### 验证

- 使用 DP 示例直接 smoke test：通过，输出已展开初始化循环。
- `ReadLints` 检查 `src/utils/codePrep.js`：无新增问题。
- `npm run build`：通过。
